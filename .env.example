# 阿里云 DashScope API Key（用于 AI 对话和 TTS 服务）
# 获取地址: https://help.aliyun.com/zh/model-studio/get-api-key
VITE_DASHSCOPE_API_KEY=your-dashscope-api-key-here

# AI 对话代理服务器地址（可选，默认为 http://localhost:8089/api/chat）
VITE_AI_PROXY_URL=http://localhost:8089/api/chat

# TTS 代理服务器地址（可选，默认为 http://localhost:8089/api/tts）
VITE_TTS_PROXY_URL=http://localhost:8089/api/tts

# 后端服务器端口（可选，默认为 3001，Docker 映射到 8089）
PORT=3001
