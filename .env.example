# 阿里云 DashScope API Key（用于 AI 对话，也可作为 NLS Token）
# 获取地址: https://help.aliyun.com/zh/model-studio/get-api-key
VITE_DASHSCOPE_API_KEY=your-dashscope-api-key-here

# 阿里云 NLS APPKEY（用于 TTS 语音合成）
# 获取地址: https://nls-portal.console.aliyun.com/applist
VITE_NLS_APPKEY=your-nls-appkey-here

# 阿里云 AccessKey（用于获取 NLS Token，推荐配置）
# 获取地址: https://ram.console.aliyun.com/manage/ak
VITE_ALIYUN_ACCESS_KEY_ID=your-access-key-id
VITE_ALIYUN_ACCESS_KEY_SECRET=your-access-key-secret

# AI 对话代理服务器地址（可选，默认为 http://localhost:8089/api/chat）
VITE_AI_PROXY_URL=http://localhost:8089/api/chat

# TTS 代理服务器地址（可选，默认为 http://localhost:8089/api/tts）
VITE_TTS_PROXY_URL=http://localhost:8089/api/tts

# ASR WebSocket 服务器地址（可选，默认为 ws://localhost:3001/ws/asr）
VITE_ASR_WS_URL=ws://localhost:3001/ws/asr

# 后端服务器端口（可选，默认为 3001，Docker 映射到 8089）
PORT=3001

# 是否启用 HTTPS（可选，默认为 false）
# 设置为 true 时，前端和后端都会使用 HTTPS
USE_HTTPS=true

# SSL 证书路径（启用 HTTPS 时需要）
# 本地开发使用相对路径
# Docker 部署时会自动映射到容器内的绝对路径
SSL_KEY_PATH=./ssl/key.pem
SSL_CERT_PATH=./ssl/cert.pem
